# ESMERY Feature Status - 01/06/2026

Tài liệu này dùng để tracking trạng thái hiện tại của app sau MVP và lượt triển khai Production V1 safety-first.

Phân loại:

- **Đã xong / chạy được trong app**: đã có UI + repository action + local state/Supabase best-effort, build/test pass.
- **Đã có contract / scaffold production**: đã có model, schema, API hook hoặc Edge Function skeleton, nhưng cần cấu hình/deploy external service để chạy thật.
- **Đang mock / UI-only**: đã có giao diện hoặc flow demo nhưng chưa production-ready.
- **Chưa xong / phase sau**: chưa triển khai hoặc mới nằm trong plan.

## 1. Đã Xong / Chạy Được Trong App

### Authentication

- Đăng nhập email/password qua Supabase Auth.
- Đăng ký tài khoản bằng tên, email và password qua Supabase Auth.
- Đăng xuất và clear local session.
- Quên mật khẩu qua `supabase.auth.resetPasswordForEmail(email)`.
- Chuyển ngôn ngữ English / Tiếng Việt trong app.

### Onboarding

- Có onboarding 3 màn:
  - `A Gentle Hand on Your Shoulder`
  - `Simple Check-ins`
  - `Feel the Warmth`
- Có flow sau đăng ký:
  - Onboarding
  - Circle setup intro
  - Safety rhythm setup intro
  - Home

### Home / Hearth

- Hiển thị tên người dùng và lần check-in gần nhất.
- Có nút `I'm Safe`.
- Khi bấm `I'm Safe`:
  - Tạo `CheckIn`.
  - Cập nhật `profile.lastSafeAt`.
  - Tạo timeline event `Check-in confirmed`.
  - Tạo notification `Check-in sent`.
  - Tạo notification delivery records trong local state.
  - Resolve/cancel active missed-check incident/job nếu có.
  - Ghi best-effort lên Supabase.
- Hiển thị trạng thái safety:
  - Safe.
  - Needs attention nếu có missed check-in hoặc emergency notification chưa đọc.
- Hiển thị notification feed gần đây.
- Mark notification as read.
- Hiển thị summary delivery/automation:
  - Pending delivery count.
  - Failed delivery count.
  - Active alert nếu có.

### My Circle

- Hiển thị danh sách người thân / bạn bè tin cậy.
- Hiển thị trạng thái member:
  - pending
  - accepted
  - declined
- Hiển thị incoming/outgoing friend requests.
- Accept / decline friend request.
- Accept request có tạo reciprocal accepted circle member trong repository.
- Tránh tạo duplicate reverse request khi đã có incoming request hoặc accepted member.
- Gửi gentle nudge cho member.
- Gentle nudge tạo:
  - Timeline event.
  - Notification.
  - Notification delivery record.
  - Supabase best-effort write.
- Có refresh Circle thủ công.

### Add Friend

- Có dialog thêm bạn.
- Hỗ trợ 3 mode nhập:
  - Contact: email hoặc phone.
  - ID.
  - QR result.
- Add Friend đi qua resolver contact/profile trong repository.
- Tạo friend request.
- Tạo pending circle member.
- Tạo timeline event.
- Ghi best-effort lên Supabase.

### Timeline

- Hiển thị lịch sử activity.
- Hỗ trợ event types:
  - check-in
  - moment
  - nudge
  - friend request
  - safety rhythm
  - missed check-in
  - emergency
- Có icon theo từng loại event.
- Có localization cho nhiều event text chính.

### Moments

- Hiển thị danh sách moments.
- Có chia sẻ moment với caption.
- Có preview ảnh bằng Coil `AsyncImage`.
- Khi share moment:
  - Tạo `Moment`.
  - Tạo timeline event.
  - Tạo notification.
  - Tạo notification delivery records.
  - Ghi best-effort lên Supabase.
- Đã sửa visibility Circle:
  - App fetch moments dựa trên RLS thay vì chỉ query `user_id = currentUser`.
  - Có migration `supabase/migrations/20260601_moment_circle_visibility.sql` để accepted Circle đọc được moment của nhau.
  - Người thân trong Circle accepted có thể thấy ảnh moment sau khi apply migration và refresh app.

### Emergency Contacts / Crisis

- Hiển thị emergency contacts.
- Thêm emergency contact.
- Xóa emergency contact.
- Toggle verified.
- Toggle auto notify.
- Gọi nhanh contact bằng Android dial intent.
- Có nút `Alert emergency contacts`.
- Khi alert emergency:
  - Tạo emergency timeline event.
  - Tạo emergency notification.
  - Tạo / update `AlertIncident`.
  - Tạo notification delivery records cho contact/circle.
  - Dựa trên danh sách contact có `autoNotify = true`.
- Crisis screen hiển thị active alert nếu có.
- Có action resolve alert incident.
- Có `My Safe Steps`.
- Có số khẩn cấp Việt Nam:
  - Police 113
  - Fire 114
  - Ambulance 115

### Safety Rhythm

- Hiển thị safety rhythm list.
- Thêm rhythm mới.
- Edit rhythm có sẵn.
- Toggle enable / pause rhythm.
- Delete rhythm.
- Lưu rhythm vào local state và Supabase best-effort.
- Có cấu hình missed check-in detection:
  - inactivity window: 2h / 4h / 12h
  - escalation delay: 15m / 30m / 60m
  - location sharing enabled flag
- Có `evaluateMissedCheckIns()`:
  - Kiểm tra `lastSafeAt`.
  - So sánh với `inactivityHours`.
  - Tạo missed check-in timeline event.
  - Tạo missed check-in notification.
  - Tạo `AlertIncident`.
  - Tạo `AlertJob`.
  - Tạo notification delivery records.
  - Chống duplicate incident/notification cho cùng mốc `lastSafeAt`.

### Subscription / Plans

- Hiển thị 3 plan:
  - Basic Care
  - Advanced Monthly
  - Advanced Yearly
- Chọn plan cập nhật `SubscriptionStatus`.
- Chọn Premium cập nhật `profile.isPremium`.
- Có `Entitlement` trong state.
- Plans screen hiển thị:
  - Plan hiện tại.
  - Premium status.
  - Entitlement source.
  - Payment order gần nhất.
- Có action tạo SePay payment order:
  - Monthly.
  - Yearly.
- Có local simulation `markPaymentOrderPaid(referenceCode)` để cập nhật entitlement.

### Notification / Delivery

- Có model `EsmeryNotification`.
- Có enum notification types:
  - check-in success
  - friend request
  - gentle nudge
  - missed check-in
  - emergency alert
  - moment shared
- Có model `NotificationDelivery`.
- Delivery channels:
  - in-app
  - push
  - sms
  - email
  - call
- Delivery statuses:
  - pending
  - sent
  - failed
  - read
- Home/Crisis đã hiển thị delivery summary cơ bản.

### Android Automation

- Có WorkManager dependency.
- Có `SafetyCheckWorker`:
  - refresh repository.
  - gọi `evaluateMissedCheckIns()`.
  - show local safety notification nếu phát hiện missed check-in.
- Có `SafetyAutomationScheduler` schedule periodic work mỗi 2 giờ.
- Có Android notification channel `esmery_safety`.
- Có `POST_NOTIFICATIONS` permission trong manifest.

## 2. Đã Có Contract / Scaffold Production

### Supabase Production V1 Contract

- Đã có migration:
  - `supabase/migrations/20260601_production_v1_contract.sql`
- Thêm bảng:
  - `device_tokens`
  - `notification_deliveries`
  - `alert_incidents`
  - `alert_jobs`
  - `location_shares`
  - `payment_orders`
  - `entitlements`
  - `audit_logs`
- Có RLS ownership policies cho các bảng mới.
- Có policy `notification-deliveries-circle-insert`.
- Có unique index chống duplicate active/escalated missed-check incident theo:
  - `user_id`
  - `reason`
  - `last_safe_at`
- Cần apply migration này lên Supabase project thật nếu chưa chạy.

### Moment Circle Visibility

- Đã có migration:
  - `supabase/migrations/20260601_moment_circle_visibility.sql`
- Policy mới `moments-circle-read` cho phép accepted Circle member đọc moment của nhau.
- Cần chạy migration này sau `esmery_v1_schema.sql`.

### Firebase Cloud Messaging

- Đã thêm dependency `firebase-messaging`.
- Có `EsmeryMessagingService`.
- Có `DeviceTokenBootstrap`.
- Có `registerDeviceToken()` / `unregisterDeviceToken()` trong repository.
- Token được lưu vào `device_tokens` qua Supabase best-effort.
- Chưa cấu hình project Firebase thật trong repo.
- Cần thêm `google-services.json` và Firebase setup trước khi push thật chạy production.

### Supabase Edge Functions

- Đã có function skeleton:
  - `supabase/functions/safety-automation/index.ts`
  - `supabase/functions/sepay-webhook/index.ts`
- `safety-automation`:
  - đọc `safety_settings`.
  - kiểm tra `profiles.last_safe_at`.
  - tạo `alert_incidents`.
  - tạo `notifications`.
  - tạo `alert_jobs`.
  - gửi push qua FCM server key nếu được cấu hình.
- `sepay-webhook`:
  - nhận webhook.
  - verify secret header.
  - tìm `payment_orders.reference_code`.
  - cập nhật order paid.
  - upsert `entitlements`.
  - upsert `subscription_status`.
  - cập nhật `profiles.is_premium`.
  - ghi `audit_logs`.
- Chưa deploy lên Supabase Functions trong repo local.
- Cần set secrets:
  - `SUPABASE_SERVICE_ROLE_KEY`
  - `FCM_SERVER_KEY`
  - `SEPAY_WEBHOOK_SECRET`

### Emergency Location Contract

- Có model `LocationShare`.
- Có repository action `shareEmergencyLocation(latitude, longitude, accuracyMeters)`.
- Có bảng `location_shares`.
- Chưa có runtime GPS permission flow và chưa lấy vị trí thật.

### Payment Contract

- Có model:
  - `PaymentOrder`
  - `Entitlement`
- Có provider:
  - Google Play
  - SePay
- Có SePay order scaffold với QR URL/reference code.
- Có SePay webhook Edge Function skeleton.
- Google Play Billing vẫn là default nên dùng cho bản phát hành Play Store.
- Chưa tích hợp Google Play Billing client thật.

## 3. Đang Mock / UI-only / Chưa Production-ready

### Local fallback data

- App vẫn có `InMemoryEsmeryRepository.seedState(...)`.
- Nếu Supabase không có data hoặc remote lỗi, app dùng mock/local seed:
  - Alex Rivers.
  - Sarah.
  - Mom.
  - Lucas request.
  - Morning coffee ritual.
  - Seed check-in/timeline/notification.

### Moment image picker

- Share moment vẫn dùng `PRESET_IMAGES` là URL Unsplash cố định.
- Chưa có chọn ảnh từ gallery/camera.
- Chưa upload ảnh lên Supabase Storage.

### QR Add Friend

- UI có mode QR.
- Chưa có camera scanner thật.
- Hiện vẫn paste QR result/code thủ công.
- Resolver backend/app đã sẵn sàng dùng chung cho camera scanner sau này.

### Push notification thật

- App đã có FCM service/token hooks.
- Chưa có Firebase project config thật trong repo.
- Chưa verify push end-to-end trên device thật.
- Chưa có Android runtime request UI cho `POST_NOTIFICATIONS`.

### Emergency alert delivery thật

- App tạo incident + delivery records.
- Chưa gửi SMS/email/call thật từ backend.
- Android hiện chỉ có dial intent thủ công cho contact.
- Chưa có retry worker/delivery provider production.

### Missed check-in automation

- App đã có WorkManager fallback.
- Edge Function skeleton đã có.
- Chưa có Supabase scheduled cron thật để gọi function định kỳ.
- Nếu chưa deploy function/cron, server-side detection chưa chạy khi user không mở app.

### Escalation delay

- Có `AlertJob` và `escalation_due_at`.
- Có incident/job creation.
- Chưa có server job processor thật để đến đúng `run_at` thì tự động escalate/gửi emergency alert.

### Location sharing

- Có flag `locationSharingEnabled`.
- Có `LocationShare` model/action/schema.
- Chưa xin runtime location permission.
- Chưa lấy GPS thật.
- Chưa map UI/live location.

### Payment đầy đủ

- Có SePay order scaffold và webhook skeleton.
- Chưa deploy webhook thật.
- Chưa verify chữ ký/HMAC theo cấu hình SePay production.
- Chưa tích hợp Google Play Billing client thật.
- Chưa receipt validation Google Play.
- Chưa enforce feature gating đầy đủ theo entitlement.

### Onboarding setup screens

- Circle setup và Rhythm setup sau onboarding vẫn là intro/continue screens.
- Chưa cho thêm contact hoặc cấu hình rhythm trực tiếp trong setup flow.

### Supabase sync depth

- Có CRUD best-effort cho entity MVP và production contract.
- Chưa có realtime subscriptions.
- Chưa có conflict resolution đầy đủ.
- Chưa có pagination.
- Chưa có offline queue/retry bền vững.

## 4. Chưa Xong / Phase Sau

### Media production

- Android Photo Picker.
- Camera capture.
- Supabase Storage upload.
- Signed/public URL policy.
- Image moderation/size limits nếu cần.

### QR scanner thật

- CameraX.
- Barcode scanner.
- Permission flow.
- Invite QR generation.

### Profile management

- Màn chỉnh sửa profile chi tiết.
- Đổi avatar.
- Đổi phone/email trong app.
- Đổi password trong app.

### Privacy / compliance

- Per-member visibility settings.
- Export/delete account flow.
- Consent screen riêng cho location/emergency sharing.
- Audit log UI cho ai đã xem trạng thái.
- Data retention policy cho location/audit/payment.

### Production payment

- Google Play Billing client.
- Google Play receipt validation backend.
- Trial/cancel/renewal flow.
- Invoice/receipt UI.
- Feature gating nghiêm theo entitlement.

### Backend automation đầy đủ

- Scheduled cron gọi `safety-automation`.
- Job processor cho `alert_jobs`.
- Retry queue cho delivery.
- Provider adapters cho SMS/email/push.
- Monitoring/logging cho delivery failure.

### Map / local support

- Runtime location permission.
- Current location.
- Map UI.
- Google Maps/Places hoặc provider tương đương.
- Tìm bệnh viện/đồn công an gần nhất.

### B2B2C / partner integrations

- Dashboard doanh nghiệp.
- Integration bảo hiểm.
- Integration y tế.
- Integration đơn vị cấp cứu.

## 5. Supabase Deployment Checklist

Chạy SQL theo thứ tự:

1. `supabase/esmery_v1_schema.sql`
2. `supabase/migrations/20260601_production_v1_contract.sql`
3. `supabase/migrations/20260601_moment_circle_visibility.sql`

Sau khi chạy SQL:

- Kiểm tra các bảng mới đã tồn tại.
- Kiểm tra RLS policy `moments-circle-read` trên `public.moments`.
- Test 2 user:
  - A invite B.
  - B accept.
  - A share moment.
  - B refresh Moments và thấy ảnh/caption của A.
- Deploy Edge Functions nếu muốn automation/payment webhook thật:
  - `safety-automation`
  - `sepay-webhook`
- Set secrets cho Edge Functions:
  - `SUPABASE_SERVICE_ROLE_KEY`
  - `FCM_SERVER_KEY`
  - `SEPAY_WEBHOOK_SECRET`
- Cấu hình scheduler/cron gọi `safety-automation`.
- Cấu hình SePay webhook URL trỏ vào function `sepay-webhook`.

## 6. Verification Hiện Tại

- `./gradlew testDebugUnitTest` pass.
- `./gradlew assembleDebug` pass.
- Có unit tests cho:
  - check-in tạo timeline + notification.
  - friend request tạo pending member.
  - duplicate friend request prevention.
  - moment + rhythm.
  - premium plan update.
  - nudge + emergency alert notification.
  - missed check-in evaluation.
  - missed check-in tạo incident/job/delivery.
  - check-in resolve active incident.
  - device token + SePay order + entitlement.
  - safety settings + emergency contact actions.
  - remote state load.
  - remote write notification sau check-in.
  - check-in delivery tới accepted circle member.
  - reciprocal circle member khi accept request.
  - no duplicate when already accepted.
  - incoming pending request reuse.
  - real user starts with empty remote state.
- Có Compose smoke tests cho:
  - Hearth
  - Circle
  - Moments
  - Safety
  - Crisis
  - Plans

## 7. Kết Luận Trạng Thái

App hiện đã vượt MVP demo và có Production V1 safety-first contract:

- Core app flow chạy được.
- Supabase schema MVP + production contract đã có.
- Moment sharing cho Circle đã có policy fix.
- WorkManager local safety check đã có.
- FCM hooks đã có nhưng cần Firebase config thật.
- Edge Function skeleton đã có nhưng cần deploy/secrets/cron.
- Payment SePay scaffold đã có nhưng cần webhook production.

Các blocker production thật còn lại chủ yếu nằm ở cấu hình external services và các integration chưa nối:

- Firebase project + FCM credentials.
- Supabase Functions deployment + cron.
- SePay webhook production.
- Google Play Billing.
- GPS/map/storage/camera scanner.
